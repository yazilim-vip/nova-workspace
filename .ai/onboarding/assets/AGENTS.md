# Workspace AGENTS.md

Workspace-specific overrides to the root `AGENTS.md`. Loaded after the root file — wins on conflicts. Populated during onboarding.

## Identity

{{One sentence. Whose workspace this is and what it's for. Example: "This workspace belongs to the acme-platform crew — product infra for Acme's retail stack."}}

## Additional Safety Rules

{{Workspace-specific non-negotiables that extend the root safety list. Delete section if none.}}

## Navigation Notes

{{Any workspace-specific navigation quirks — e.g. a mandatory bootstrap script, a non-default repo layout, extra files the agent must read on session start. Delete section if none.}}

## Local Conventions

{{Anything else that should shape every session in this workspace but isn't infra (which belongs in `.ai/workspace/infra.md`) or persona (which belongs in `.ai/workspace/SOUL.md`).}}
