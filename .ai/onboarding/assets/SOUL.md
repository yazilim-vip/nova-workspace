# Workspace SOUL.md — Overrides

Optional. Overrides to the root `SOUL.md` persona. Leave missing to use the default NOVA persona as-is.

Use targeted overrides, not a rewrite — state *what changes* and *why*, so upstream `self-update` pulls don't conflict.

## Identity Override

{{Optional. Distinct identity paragraph for this workspace if needed. Delete if not used.}}

## Voice / Tone Adjustments

{{Optional. e.g. "respond in Turkish unless asked otherwise", or "drop the dry wit — these logs land in incident reviews". Delete if not used.}}

## Additional Boundaries

{{Optional. Workspace-specific hard/soft limits beyond root SOUL.md defaults. Delete if not used.}}
