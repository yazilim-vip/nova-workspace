@../AGENTS.md

# NOVA Workspace — Claude Adapter

This file is generated by NOVA's adapters procedure (`.ai/adapters/README.md`) and lives at `.claude/CLAUDE.md` — gitignored, per-developer, regenerable. It is **not** a replacement for `AGENTS.md`; the `@AGENTS.md` import above loads the framework's real instructions into your context at session start.

## Why this file exists

Claude Code reads `CLAUDE.md` natively but does **not** read `AGENTS.md` unless explicitly imported. The repository's committed root `CLAUDE.md` currently uses a plain text instruction ("Always read AGENTS.md") — that's context, not enforcement. This adapter strengthens the guarantee with the `@AGENTS.md` import above, which Claude Code expands into context at launch, the same way it loads any `CLAUDE.md`.

## Anti-duplication rule (strict)

Every rule NOVA enforces — safety, navigation, identity, skills, workspace overrides — lives under `AGENTS.md` or `.ai/`. This adapter is a **pointer**, not a copy.

- **Reference, never reproduce.** Do not paraphrase safety or navigation rules into chat responses, generated code comments, or new files. Reference the source path (`AGENTS.md:<line>`, `.ai/workspace/AGENTS.md:<line>`).
- **Do not add Claude-specific rules to this file.** If a rule is Claude-specific enough to warrant strict behavior, put it at `.ai/adapters/claude/<rule>.md` and reference it here with an `@` import — same pattern Kiro uses for `.ai/adapters/kiro/terminal.md`.
- **If a rule applies to all agents, it belongs in `AGENTS.md` or `.ai/`**, not here. This adapter does not own any rule.

## What to read, in order

The `@AGENTS.md` import above handles #1 automatically. The rest you load on demand per the Navigation Protocol in `AGENTS.md`:

1. `AGENTS.md` — framework defaults. **Auto-loaded via `@` import.**
2. `.ai/workspace/AGENTS.md` — workspace identity and overrides. Read on session start; if missing, run `.ai/onboarding/README.md`.
3. `.ai/workspace/map/repos.md` — repository map. Never guess paths.
4. Per-repo `AGENTS.md` — when entering a specific repo under `git-repositories/`.
5. Skills, learnings, procedures — on demand per the Navigation Protocol.

Do not preload. Do not guess. Do not duplicate.
