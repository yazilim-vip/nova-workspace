@./AGENTS.md

# {{REPO_NAME}} — Claude shim

Generated by the NOVA adapters procedure (`.ai/adapters/README.md`). Lives at `git-repositories/{{REPO_PATH}}/.claude/CLAUDE.md` — gitignored, per-developer, regenerable.

Claude Code walks parent directories for `CLAUDE.md` and discovers subdirectory shims. When you open or edit a file under this repo, this shim adds the repo's `AGENTS.md` to context via the `@` import above — Navigation Protocol step 4 handled automatically.

Do not edit. Do not paraphrase. The source of truth is this repo's `AGENTS.md`.
